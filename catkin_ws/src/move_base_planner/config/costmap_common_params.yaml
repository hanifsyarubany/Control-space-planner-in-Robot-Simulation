# Robot footprint (essential)
footprint: [[-0.201, -0.200], [-0.201, 0.200], [0.205, 0.200], [0.205, -0.200]]

# Inflation parameters (can be overridden per costmap if needed)
inflation_radius: 0.5
cost_scaling_factor: 3.0

# Map type
map_type: costmap

transform_tolerance: 1.0